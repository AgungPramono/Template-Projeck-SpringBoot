package com.agung.biodata;

import com.agung.template.TemplateProjekApplication;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.SpringApplicationConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

/**
 * Unit test for simple App.
 */
@RunWith(SpringJUnit4ClassRunner.class)
@SpringApplicationConfiguration(classes = TemplateProjekApplication.class)
public class TemplateProjeckApplicationTest {
    @Test
	public void contextLoads() {
            //Mahasiswa m = md.findByNpm("13.1.02.03.0340");
            //Assert.assertNotNull(m);
	}
}
 